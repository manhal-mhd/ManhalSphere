# Mailu configuration template for Office-in-a-Box
# Copy to `mailu.env` and adjust values before first deployment.

# Core
MAILU_VERSION=2.0
SECRET_KEY=replace_with_a_long_random_secret
SUBNET=172.30.0.0/16

# Domain and hostnames
DOMAIN=example.com
HOSTNAMES=mail.example.com
POSTMASTER=postmaster

# TLS mode: cert | letsencrypt | notls
TLS_FLAVOR=cert

# Webmail and admin
WEBMAIL=roundcube
SITENAME=Office-in-a-Box Mail

# Optional limits and tuning
MESSAGE_SIZE_LIMIT=50000000
AUTH_RATELIMIT=10/minute;1000/hour

# Security / behavior
WELCOME=false
WEB_ADMIN=/admin
WEB_WEBMAIL=/webmail